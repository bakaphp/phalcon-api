# Codeception Test Suite Configuration
#
# Suite for API tests.

actor: ApiTester
modules:
  enabled:
    - Asserts
    - Helper\Api
    - \Helper\Integration
    - REST:
        depends: PhpBrowser
        url: 'http://127.0.0.1'
        curl:
          CURLOPT_RETURNTRANSFER: true